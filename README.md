# personla-neovim-config

The NeoVim configuration written in lua for personal development.

## TODO
  - Drop `lzp-zero`
  - Drop `null-ls`
  - Migrate from `packer` to `lazy`
  - status line configuration
